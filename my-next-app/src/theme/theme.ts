// src/theme/theme.ts
import {createTheme} from 'shadcn-ui/dist';

const theme = createTheme({
  colors: {
    primary: '#0070f3',
    secondary: '#1a202c',
    // Add other theme configurations here
  },
  // Add other theme configurations here
});

export default theme;
